```mermaid
@startuml
' --- 锦城学院黑白学术风配置 ---
skinparam monochrome true
skinparam shadowing false
skinparam defaultFontName "Microsoft YaHei"
skinparam componentStyle rectangle
skinparam linetype ortho
skinparam nodesep 40
skinparam ranksep 60
skinparam defaultTextAlignment center
skinparam packageTitleAlignment center

' --- 节点定义与内部结构 ---
package "表现层_前端视图" {
    component "Web浏览器交互端" as Browser
    component "HTML5/CSS3\n布局响应" as UI
    component "Chart.js\n动态指标图表" as Chart
    component "JavaScript\n正则安全校验" as JS
    
    Browser -down-> UI
    Browser -down-> Chart
    Browser -down-> JS
}

package "数据持久层_底层存储" {
    database "MySQL\n关系型数据库" as DB
    component "本地静态\n文件系统" as FS
}

package "逻辑控制层_后端服务" {
    component "Flask Web 调度框架" as Flask
    component "PyTorch 深度学习引擎" as PyTorch
    component "OpenCV 图像处理模块" as OpenCV
    component "APE-GAN 生成器模型" as APE
    component "对抗样本攻击模拟器" as Attack
    
    Flask -down-> PyTorch
    Flask -down-> OpenCV
    PyTorch -down-> APE
    PyTorch -down-> Attack
}

' --- 跨层交互连接 ---
UI <.[bold].> Flask : " HTTP/JSON 异步请求 "
Flask <.[bold].> DB : " PyMySQL 连接池 "
Flask <.[bold].> FS : " I/O 读写 "

@enduml
```